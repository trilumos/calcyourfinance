/**
 * FD (Fixed Deposit) calculator formula.
 * Uses compoundFutureValue() with no contributions — lump-sum compounding only.
 */
export { compoundFutureValue } from "../_shared/finance";
export type { CompoundFVResult } from "../_shared/finance";
