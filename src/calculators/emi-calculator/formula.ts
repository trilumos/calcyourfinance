/**
 * EMI calculator formula — thin re-export of the shared emi() function.
 * Pure math; no formatting, no side effects.
 */
export { emi } from "../_shared/finance";
export type { EmiResult } from "../_shared/finance";
